
Teacher Manager

To start a webserver hosting the application, you can launch either of the runscripts (depending on your system). This requires Python 3

Launch your browser and enter the address/port of the webserver. The default address/port is

	http://localhost:8000
